import React from 'react';
import { StyledTitle } from '../../styles';

const Error = (props) => {
  console.log('Error');
  return <StyledTitle>Houve um erro ao processar sua requisiçaõ</StyledTitle>;
};

export default Error;
